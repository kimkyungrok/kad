<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">

  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title><%= typeof title !== 'undefined' ? title : 'MySite' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      margin: 0;
      font-family: "배달의민족 주아 OTF";
      font-size: 16px;
    }

    input[name="username"],
    input[name="password"],
    input[name="passwordConfirm"] {
      font-family: Arial !important;
      font-weight: bold;
    }

    .split-layout {
      display: flex;
      flex-direction: row;
    }

    .top-bar {
      position: fixed;
      top: 65px; /* header 아래에 위치 */
      left: 0;
      width: 100%;
      height: auto;
      background-color: #f8f9fa;
      display: flex;
      align-items: center;
      justify-content: flex-start;
      padding: 0 30px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      z-index: 1000;
    }

    .main-content {
      margin-top: 100px; /* header + top-bar 높이 */
      padding: 20px;
      width: 100%;
    }

     @media (max-width: 876px) {
        .top-bar {
          flex-direction: column;
          height: auto;
          padding: 10px;
          gap: 10px;

          transform: scale(0.7);         /* 전체 축소 */
          transform-origin: top left;
          width: 167%;                   /* scale 보정용 */
        }

        .main-content {
          margin-top: 160px;             /* 위 요소 줄어든 만큼 조정 */
        }

        body {
          transform: scale(0.65);
          transform-origin: top left;
          width: 167%; /* scale 보정 */
        }
      }
  </style>

</head>
<body>

  <%- include('partials/navbar') %>

  <div class="container mt-4">
    <%- body %>
  </div>

  <!-- footerbar 포함 -->
  <%- include('partials/footerbar') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const footer = document.getElementById("footerBar");
    if (footer) {
      const footerHeight = footer.offsetHeight;
      document.body.style.paddingBottom = footerHeight + "px";
    }
  });
</script>