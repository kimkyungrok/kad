<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">

  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title><%= typeof title !== 'undefined' ? title : 'MySite' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- PWA 매니페스트 -->
  <link rel="manifest" href="/manifest.json">

  <!-- 파비콘 (브라우저 탭) -->
  <link rel="icon" href="/icons/KAD.png" sizes="192x192" type="image/png">

  <!-- iOS 홈 화면용 터치 아이콘 -->
  <link rel="apple-touch-icon" href="/icons/KAD.png" sizes="192x192">

  <!-- Android Chrome 색상바 -->
  <meta name="theme-color" content="#0d6efd">

  <!-- iOS 웹앱 모드 활성화 -->
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">

<style>
  body {
    margin: 0;
    font-family: "배달의민족 주아 OTF", Arial, sans-serif;
    font-size: 16px;
  }

  input[name="username"],
  input[name="password"],
  input[name="passwordConfirm"] {
    font-family: Arial !important;
    font-weight: bold;
  }

  .split-layout {
    display: flex;
    flex-direction: row;
  }

  .top-bar {
    position: fixed;
    top: 45px; /* header 아래에 위치 */
    left: 0;
    width: 100%;
    background-color: #d6eaff;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: flex-start;
    padding: 10px 30px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    z-index: 1000;
  }

  .main-content {
    margin-top: 130px; /* top-bar + header 높이 보정 */
    padding: 20px;
    width: 100%;
  }

  @media (max-width: 876px) {
    .top-bar {
      flex-direction: column;
      align-items: flex-start;
      padding: 10px 15px;
    }

    .top-bar input,
    .top-bar label,
    .top-bar button {
      font-size: 0.9rem;
    }
    
    .top-bar .form-control-sm {
      min-width: 100%;
    }

    .main-content {
      margin-top: 150px; /* 모바일에서 top-bar가 늘어나는 것에 대비 */
    }
  }
</style>

</head>
<body>

  <%- include('partials/navbar') %>

  <div class="container mt-4">
    <%- body %>
  </div>

  <!-- footerbar 포함 -->
  <%- include('partials/footerbar') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const footer = document.getElementById("footerBar");
    if (footer) {
      const footerHeight = footer.offsetHeight;
      document.body.style.paddingBottom = footerHeight + "px";
    }
  });
</script>